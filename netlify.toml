[build]
  command = "mkdir -p dist && echo 'ok' > dist/index.html"
  publish = "dist"
  edge_functions = "netlify/edge-functions"

[build.environment]
  NODE_VERSION = "18"

[[edge_functions]]
  path = "/api/chat"
  function = "chat"
